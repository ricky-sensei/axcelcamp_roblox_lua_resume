# Roblox Lua 基礎②  
# 計算と演算子

---

## 🎯 今日の目標

- 計算ができるようになる  
- 「くらべる」ことができるようになる  
- 条件分岐（if文）につながる準備をする  

---

# 1. 計算ってなに？

ゲームの中では、計算がめちゃくちゃ使われています。

たとえば：

- ダメージを受けたらHPを減らす  
- コインを取ったらスコアを増やす  
- 時間を数える  

こういう処理は **計算（演算）** で作られています。

---

# 2. 算術演算子（計算の記号）

Luaでは、次の記号を使って計算します。

| 記号 | 意味 | 例 |
|---|---|---|
| + | 足す | 5 + 3 |
| - | 引く | 10 - 4 |
| * | かける | 2 * 3 |
| / | 割る | 8 / 2 |

---

## 実際に計算してみよう

```lua
local a = 10
local b = 3

print(a + b)  -- 13
print(a - b)  -- 7
print(a * b)  -- 30
print(a / b)  -- 3.333...
```

---

## よくある使い方（HP）}

```lua
local hp = 100
local damage = 20

hp = hp - damage

print(hp)  -- 80
```

---

# 3. 比較演算子（くらべる）

| 記号 | 意味 |
|---|---|
| == | 同じ |
| ~= | ちがう |
| > | 大きい |
| < | 小さい |
| >= | 以上 |
| <= | 以下 |

---

## 比較してみよう

```lua
local score = 100

print(score == 100)   -- true
print(score ~= 50)    -- true
print(score > 80)     -- true
print(score < 80)     -- false
```

---

## ⚠ 超重要な注意

```lua
score = 100    -- 代入
score == 100   -- 比較
```

`=` と `==` は **まったく別物**。

---

# 4. 論理演算子（組み合わせる）

| 書き方 | 意味 |
|---|---|
| and | どっちもOK |
| or | どっちかOK |
| not | 反対 |

---

## 条件を組み合わせる例

```lua
local hp = 50
local hasKey = true

print(hp > 0 and hasKey)   -- true
print(hp <= 0 or hasKey)  -- true
```

---

## not の例

```lua
local isDead = false

print(not isDead)  -- true
```

---

# 5. if文につながる例（予告）

```lua
local hp = 0

if hp <= 0 then
    print("ゲームオーバー")
end
```

---

# 6. Robloxで実験してみよう

```lua
local score = 0

score = score + 10
print("スコア: " .. score)

score = score + 10
print("スコア: " .. score)
```

---

# 7. まとめ

- `+ - * /` で計算できる  
- `== ~= > <` でくらべられる  
- 結果は true / false  
- if文はこの上に成り立っている  

---

# 🎮 ミニチャレンジ

1. HPを100にして、30ダメージを2回与えてみよう  
2. score が 50 以上かどうかを print してみよう  
3. 「HPが0以下 かつ ゲームオーバーでない」条件を考えてみよう  
